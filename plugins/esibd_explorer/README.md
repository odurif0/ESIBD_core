This directory is intended to be used as the ESIBD Explorer `plugin path`.

Each subdirectory contains one external plugin package that ESIBD Explorer can
discover and load.

Current AMPR variants:
- `ampr_a/`: autonomous AMPR_A plugin
- `ampr_b/`: autonomous AMPR_B plugin
- `dmmr/`: autonomous DMMR plugin
